<div class="inbox-mockup" aria-hidden="true">
    <div class="ambient-glow"></div>

    <div class="window-wrapper">
        <div class="callout callout-top-right">
            <span class="callout-emoji" aria-hidden="true">⚡</span>
            <span>Instant categorization</span>
        </div>

        <div class="window-frame">
            <div class="luminous-frame" aria-hidden="true">
                <div class="luminous-border" data-animate></div>
                <div class="luminous-glow" data-animate></div>
                <div class="luminous-corner luminous-corner-tr" data-animate>
                </div>
                <div class="luminous-corner luminous-corner-bl" data-animate>
                </div>
            </div>

            <div class="window" data-animate>
                <div class="window-highlight" aria-hidden="true"></div>

                <div class="window-header">
                    <div class="traffic">
                        <span class="traffic-dot traffic-red"></span>
                        <span class="traffic-dot traffic-yellow"></span>
                        <span class="traffic-dot traffic-green"></span>
                    </div>

                    <div class="window-title">Inbox</div>

                    <div class="window-badge">
                        <svg
                            class="gmail-icon"
                            viewBox="52 42 88 66"
                            width="20"
                            height="16"
                            aria-hidden="true"
                        >
                            <path
                                fill="#4285f4"
                                d="M58 108h14V74L52 59v43c0 3.32 2.69 6 6 6"
                            ></path>
                            <path
                                fill="#34a853"
                                d="M120 108h14c3.32 0 6-2.69 6-6V59l-20 15"
                            ></path>
                            <path
                                fill="#fbbc04"
                                d="M120 48v26l20-15v-8c0-7.42-8.47-11.65-14.4-7.2"
                            ></path>
                            <path
                                fill="#ea4335"
                                d="M72 74V48l24 18 24-18v26L96 92"></path>
                            <path
                                fill="#c5221f"
                                d="M52 51v8l20 15V48l-5.6-4.2c-5.94-4.45-14.4-.22-14.4 7.2"
                            ></path>
                        </svg>
                        <span>Gmail</span>
                    </div>
                </div>

                <div class="window-body">
                    <div class="sidebar" aria-hidden="true">
                        <div class="sidebar-icon sidebar-icon-active">
                            <svg
                                width="18"
                                height="14"
                                viewBox="0 0 20 16"
                                fill="#60a5fa"
                                aria-hidden="true"
                            >
                                <path
                                    d="M18 0H2C0.9 0 0 0.9 0 2V14C0 15.1 0.9 16 2 16H18C19.1 16 20 15.1 20 14V2C20 0.9 19.1 0 18 0ZM18 4L10 9L2 4V2L10 7L18 2V4Z"
                                ></path>
                            </svg>
                        </div>

                        <div class="sidebar-icon">
                            <svg
                                width="18"
                                height="18"
                                viewBox="0 0 20 20"
                                fill="#52525b"
                                aria-hidden="true"
                            >
                                <path
                                    d="M10 0C4.5 0 0 4.5 0 10C0 15.5 4.5 20 10 20C15.5 20 20 15.5 20 10C20 4.5 15.5 0 10 0ZM10 18C5.6 18 2 14.4 2 10C2 5.6 5.6 2 10 2C14.4 2 18 5.6 18 10C18 14.4 14.4 18 10 18ZM10.5 5H9V11L14.2 14.2L15 12.9L10.5 10.2V5Z"
                                ></path>
                            </svg>
                        </div>

                        <div class="sidebar-icon">
                            <svg
                                width="14"
                                height="18"
                                viewBox="0 0 16 20"
                                fill="#52525b"
                                aria-hidden="true"
                            >
                                <path
                                    d="M14 0H2C0.9 0 0 0.9 0 2V20L8 16L16 20V2C16 0.9 15.1 0 14 0ZM14 17L8 14.2L2 17V2H14V17Z"
                                ></path>
                            </svg>
                        </div>
                    </div>

                    <div class="emails">
                        <div class="email email-1">
                            <div class="avatar avatar-pink">A</div>

                            <div class="email-content">
                                <div class="email-top">
                                    <span class="email-from email-from-strong">
                                        Alex Rivera
                                    </span>
                                    <span class="pill pill-red">To Respond</span
                                    >
                                    <span class="email-time">3:42 PM</span>
                                </div>

                                <div class="email-subject email-subject-strong">
                                    Re: Product Launch Timeline
                                </div>

                                <div class="email-preview">
                                    Hey! Wanted to check in on the launch
                                    assets. Are we still on track for next
                                    week...
                                </div>

                                <div class="autodraft">
                                    <div class="autodraft-label">
                                        <span>Autodraft</span>
                                        <span class="autodraft-dot"></span>
                                    </div>
                                    <div class="autodraft-text">
                                        <span class="typing-line">Hi Alex! Yes, we're on track. The design team finished the assets yesterday and I'll send over the final files by EOD today.</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="email email-2">
                            <div class="avatar avatar-blue">E</div>

                            <div class="email-content">
                                <div class="email-top">
                                    <span class="email-from">Emma Park</span>
                                    <span class="pill pill-blue">Meeting</span>
                                    <span class="email-time email-time-dim">
                                        2:18 PM
                                    </span>
                                </div>

                                <div class="email-subject">
                                    Quick sync this afternoon?
                                </div>

                                <div class="email-preview email-preview-dim">
                                    Would love to catch up on the roadmap
                                    updates. Are you free around 4pm...
                                </div>
                            </div>
                        </div>

                        <div class="email email-3">
                            <div class="avatar avatar-purple">M</div>

                            <div class="email-content">
                                <div class="email-top">
                                    <span class="email-from email-from-dim">
                                        Marketing
                                    </span>
                                    <span class="pill pill-amber">FYI</span>
                                    <span class="email-time email-time-dim">
                                        10:05 AM
                                    </span>
                                </div>

                                <div class="email-subject email-subject-dim">
                                    Campaign Results Ready
                                </div>
                            </div>
                        </div>

                        <div class="email email-4">
                            <div class="avatar avatar-lavender">S</div>

                            <div class="email-content">
                                <div class="email-top">
                                    <span class="email-from email-from-faint">
                                        Shopify
                                    </span>
                                    <span class="pill pill-lavender"
                                        >Promotions</span
                                    >
                                    <span class="email-time email-time-dim">
                                        Yesterday
                                    </span>
                                </div>

                                <div class="email-subject email-subject-faint">
                                    Your store is ready for Black Friday
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="callout callout-bottom-left">
            <span class="callout-emoji callout-emoji-purple" aria-hidden="true"
                >✨</span
            >
            <span>AI-powered replies</span>
        </div>
    </div>
</div>

<style>
    .inbox-mockup {
        width: 100%;
        height: 100%;
        position: relative;
        display: flex;
        justify-content: center;
        padding: 10px 40px;
        box-sizing: border-box;
        font-family:
            -apple-system, BlinkMacSystemFont, "SF Pro Display", system-ui,
            sans-serif;
        contain: layout style;
    }

    @media (max-width: 1199px) {
        .inbox-mockup {
            padding: 40px 24px;
        }
    }

    @media (max-width: 809px) {
        .inbox-mockup {
            padding: 20px 16px;
        }
    }

    .ambient-glow {
        position: absolute;
        top: 15%;
        left: 50%;
        transform: translateX(-50%);
        width: 70%;
        height: 60%;
        background: radial-gradient(
            ellipse 100% 80% at 50% 60%,
            rgba(59, 130, 246, 0.1) 0%,
            rgba(139, 92, 246, 0.05) 40%,
            transparent 70%
        );
        filter: blur(50px);
        z-index: 0;
        pointer-events: none;
    }

    .window-wrapper {
        position: relative;
        width: 100%;
        max-width: 880px;
        margin: 0 auto;
        z-index: 1;
    }

    @media (max-width: 1199px) {
        .window-wrapper {
            max-width: calc(100% - 120px);
        }
    }

    @media (max-width: 809px) {
        .window-wrapper {
            max-width: 100%;
        }
    }

    .callout {
        --callout-in-delay: 1.2s;
        --callout-float-delay: 1.8s;
        position: absolute;
        z-index: 20;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 10px 18px;
        border-radius: 20px;
        padding: 10px 18px;
        border-radius: 20px;
        background: var(--mockup-window-bg);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        box-shadow: var(--mockup-shadow);
        border: 1px solid var(--mockup-border);
        font-size: 13px;
        font-weight: 500;
        color: var(--duet-fg);
        letter-spacing: -0.01em;
        opacity: 0;
        transform: scale(0.9);
        animation:
            calloutIn 0.5s cubic-bezier(0.23, 1, 0.32, 1)
                var(--callout-in-delay) forwards,
            calloutFloat 4s ease-in-out var(--callout-float-delay) infinite;
        will-change: transform, opacity;
    }

    @media (max-width: 1199px) {
        .callout {
            gap: 6px;
            padding: 8px 14px;
            border-radius: 16px;
            font-size: 12px;
        }
    }

    .callout-emoji {
        filter: drop-shadow(0 0 4px rgba(250, 204, 21, 0.4));
    }

    .callout-emoji-purple {
        filter: drop-shadow(0 0 4px rgba(168, 85, 247, 0.4));
    }

    .callout-top-right {
        top: 80px;
        right: -60px;
    }

    .callout-bottom-left {
        bottom: 65px;
        left: -60px;
        --callout-float-delay: 2.1s;
    }

    @media (min-width: 1200px) {
        .callout-top-right {
            top: 90px;
        }

        .callout-bottom-left {
            bottom: 147px;
        }
    }

    @media (max-width: 809px) {
        .callout {
            display: none;
        }
    }

    @keyframes calloutFloat {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-6px);
        }
    }

    @keyframes calloutIn {
        from {
            opacity: 0;
            transform: scale(0.9);
            filter: blur(2px);
        }
        to {
            opacity: 1;
            transform: scale(1);
            filter: blur(0px);
        }
    }

    .window-frame {
        position: relative;
    }

    .luminous-frame {
        position: absolute;
        inset: -10px;
        border-radius: 26px;
        pointer-events: none;
        z-index: 0;
        opacity: 0;
        animation: frameFadeIn 0.8s ease-out 0.3s forwards;
    }

    @media (max-width: 1199px) {
        .luminous-frame {
            border-radius: 22px;
        }
    }

    @media (max-width: 809px) {
        .luminous-frame {
            inset: -8px;
            border-radius: 18px;
        }
    }

    .luminous-border {
        position: absolute;
        inset: 0;
        border-radius: inherit;
        padding: 1.5px;
        background: linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.6) 0%,
            rgba(180, 180, 190, 0.5) 25%,
            rgba(255, 255, 255, 0.5) 50%,
            rgba(160, 160, 170, 0.5) 75%,
            rgba(255, 255, 255, 0.6) 100%
        );
        -webkit-mask:
            linear-gradient(#fff 0 0) content-box,
            linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        opacity: 0.5;
        will-change: opacity;
        animation: frameBorderPulse 8s ease-in-out infinite;
    }

    .luminous-glow {
        position: absolute;
        inset: -4px;
        border-radius: 30px;
        background: transparent;
        box-shadow:
            0 0 20px 4px rgba(120, 120, 130, 0.12),
            0 0 40px 8px rgba(200, 200, 210, 0.06),
            inset 0 0 20px 4px rgba(255, 255, 255, 0.05);
        opacity: 0.4;
        will-change: opacity;
        animation: frameGlowPulse 8s ease-in-out 0.5s infinite;
    }

    @media (max-width: 1199px) {
        .luminous-glow {
            border-radius: 26px;
        }
    }

    @media (max-width: 809px) {
        .luminous-glow {
            border-radius: 22px;
        }
    }

    .luminous-corner {
        position: absolute;
        width: 60px;
        height: 60px;
        border-radius: inherit;
        opacity: 0.4;
        will-change: opacity;
        animation: cornerPulse 6s ease-in-out infinite;
    }

    @media (max-width: 809px) {
        .luminous-corner {
            width: 40px;
            height: 40px;
        }
    }

    .luminous-corner-tr {
        top: -2px;
        right: -2px;
        background: radial-gradient(
            ellipse at 100% 0%,
            rgba(255, 255, 255, 0.5) 0%,
            transparent 70%
        );
        animation-delay: 1.5s;
    }

    .luminous-corner-bl {
        bottom: -2px;
        left: -2px;
        background: radial-gradient(
            ellipse at 0% 100%,
            rgba(200, 200, 210, 0.4) 0%,
            transparent 70%
        );
        animation-delay: 3s;
    }

    .window {
        width: 100%;
        background: var(--mockup-window-bg);
        backdrop-filter: blur(40px);
        -webkit-backdrop-filter: blur(40px);
        border-radius: 16px;
        box-shadow:
            var(--mockup-shadow),
            0 0 0 1px var(--mockup-border);
        overflow: hidden;
        position: relative;
        z-index: 1;
        opacity: 0;
        transform: translateY(24px);
        animation: windowIn 0.7s cubic-bezier(0.23, 1, 0.32, 1) 0.15s forwards;
    }

    @media (max-width: 1199px) {
        .window {
            border-radius: 12px;
        }
    }

    @media (max-width: 809px) {
        .window {
            border-radius: 10px;
            box-shadow:
                0 20px 40px rgba(0, 0, 0, 0.3),
                0 0 0 1px rgba(255, 255, 255, 0.08);
            transform: translateY(16px);
        }
    }

    .window-highlight {
        position: absolute;
        top: 0;
        left: 10%;
        right: 10%;
        height: 1px;
        background: linear-gradient(
            90deg,
            transparent,
            var(--mockup-border),
            transparent
        );
        pointer-events: none;
    }

    .window-header {
        height: 48px;
        background: transparent;
        border-bottom: 1px solid var(--mockup-border);
        display: flex;
        align-items: center;
        padding: 0 18px;
        position: relative;
        color: var(--duet-muted);
    }

    @media (max-width: 1199px) {
        .window-header {
            height: 42px;
            padding: 0 14px;
        }
    }

    @media (max-width: 809px) {
        .window-header {
            height: 38px;
            padding: 0 12px;
        }
    }

    .traffic {
        display: flex;
        gap: 8px;
    }

    @media (max-width: 1199px) {
        .traffic {
            gap: 6px;
        }
    }

    @media (max-width: 809px) {
        .traffic {
            gap: 5px;
        }
    }

    .traffic-dot {
        width: 12px;
        height: 12px;
        border-radius: 9999px;
        box-shadow: inset 0 -1px 2px rgba(0, 0, 0, 0.2);
    }

    @media (max-width: 1199px) {
        .traffic-dot {
            width: 10px;
            height: 10px;
        }
    }

    @media (max-width: 809px) {
        .traffic-dot {
            width: 8px;
            height: 8px;
        }
    }

    .traffic-red {
        background: linear-gradient(135deg, #ff6b6b 0%, #ee5a5a 100%);
    }

    .traffic-yellow {
        background: linear-gradient(135deg, #ffd93d 0%, #f5c842 100%);
    }

    .traffic-green {
        background: linear-gradient(135deg, #6bcb77 0%, #5cb868 100%);
    }

    .window-title {
        margin-left: 16px;
        font-size: 13px;
        font-weight: 500;
        letter-spacing: -0.01em;
    }

    @media (max-width: 1199px) {
        .window-title {
            margin-left: 12px;
            font-size: 12px;
        }
    }

    @media (max-width: 809px) {
        .window-title {
            margin-left: 10px;
            font-size: 11px;
        }
    }

    .window-badge {
        margin-left: auto;
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 12px;
        font-weight: 500;
        letter-spacing: -0.01em;
        color: var(--duet-muted);
    }

    @media (max-width: 1199px) {
        .window-badge {
            gap: 6px;
            font-size: 11px;
        }
    }

    @media (max-width: 809px) {
        .window-badge {
            gap: 4px;
            font-size: 10px;
        }
    }

    .gmail-icon {
        width: 20px;
        height: 16px;
    }

    @media (max-width: 1199px) {
        .gmail-icon {
            width: 18px;
            height: 14px;
        }
    }

    @media (max-width: 809px) {
        .gmail-icon {
            width: 16px;
            height: 12px;
        }
    }

    .window-body {
        display: flex;
        height: 333px;
        overflow: hidden;
    }

    @media (max-width: 1199px) {
        .window-body {
            height: 320px;
        }
    }

    @media (max-width: 809px) {
        .window-body {
            height: 285px;
        }
    }

    .sidebar {
        width: 58px;
        background: transparent;
        border-right: 1px solid var(--mockup-border);
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 14px 0;
        gap: 6px;
    }

    @media (max-width: 1199px) {
        .sidebar {
            display: none;
        }
    }

    .sidebar-icon {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .sidebar-icon-active {
        background: rgba(59, 130, 246, 0.15);
        border: 1px solid rgba(59, 130, 246, 0.25);
    }

    .emails {
        flex: 1;
        min-height: 0;
        display: flex;
        flex-direction: column;
        overflow: hidden;
    }

    .email {
        --delay: 0.3s;
        --final-opacity: 1;
        display: flex;
        gap: 14px;
        padding: 18px 20px;
        opacity: 0;
        transform: translateY(16px);
        animation: emailIn 0.6s cubic-bezier(0.23, 1, 0.32, 1) var(--delay)
            forwards;
    }

    @media (max-width: 1199px) {
        .email {
            gap: 12px;
        }
    }

    @media (max-width: 809px) {
        .email {
            gap: 10px;
        }
    }

    .email-1 {
        --delay: 0.3s;
        --final-opacity: 1;
        background: var(--email-bg-1);
        border-bottom: 1px solid var(--email-border);
    }

    .email-2 {
        --delay: 0.42s;
        --final-opacity: 0.55;
        border-bottom: 1px solid var(--mockup-border);
    }

    .email-3 {
        --delay: 0.54s;
        --final-opacity: 0.4;
        border-bottom: 1px solid var(--mockup-border);
    }

    .email-4 {
        --delay: 0.66s;
        --final-opacity: 0.25;
    }

    .avatar {
        width: 42px;
        height: 42px;
        border-radius: 9999px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 16px;
        font-weight: 600;
        color: white;
        flex-shrink: 0;
    }

    @media (max-width: 1199px) {
        .avatar {
            width: 36px;
            height: 36px;
            font-size: 14px;
        }
    }

    @media (max-width: 809px) {
        .avatar {
            width: 32px;
            height: 32px;
            font-size: 13px;
        }
    }

    .avatar-pink {
        background: linear-gradient(135deg, #f472b6 0%, #ec4899 100%);
        box-shadow: 0 4px 12px rgba(244, 114, 182, 0.25);
    }

    .avatar-blue {
        background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
    }

    .avatar-purple {
        background: linear-gradient(135deg, #a78bfa 0%, #8b5cf6 100%);
    }

    .avatar-lavender {
        background: linear-gradient(135deg, #b99aff 0%, #a78bfa 100%);
        width: 30px;
        height: 30px;
        font-size: 11px;
    }

    @media (max-width: 1199px) {
        .avatar-lavender {
            width: 26px;
            height: 26px;
            font-size: 10px;
        }
    }

    @media (max-width: 809px) {
        .avatar-lavender {
            width: 22px;
            height: 22px;
            font-size: 9px;
        }
    }

    .email-content {
        flex: 1;
        min-width: 0;
    }

    .email-top {
        display: flex;
        align-items: center;
        gap: 10px;
        flex-wrap: wrap;
        margin-bottom: 6px;
    }

    @media (max-width: 809px) {
        .email-top {
            gap: 6px;
            margin-bottom: 4px;
        }
    }

    .email-from {
        font-size: 13px;
        font-weight: 500;
        color: var(--duet-muted);
        letter-spacing: -0.01em;
    }

    .email-from-strong {
        font-size: 14px;
        font-weight: 600;
        color: var(--duet-fg);
    }

    .email-from-dim {
        color: #71717a;
        font-size: 12px;
    }

    .email-from-faint {
        color: #52525b;
        font-size: 11px;
    }

    @media (max-width: 809px) {
        .email-from {
            font-size: 12px;
        }

        .email-from-strong {
            font-size: 12px;
        }

        .email-from-dim {
            font-size: 10px;
        }

        .email-from-faint {
            font-size: 9px;
        }
    }

    .email-time {
        margin-left: auto;
        font-size: 11px;
        color: var(--duet-muted);
        font-weight: 500;
    }

    .email-time-dim {
        color: #3f3f46;
    }

    @media (max-width: 809px) {
        .email-time {
            font-size: 9px;
        }
    }

    .pill {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-size: 10px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        padding: 3px 10px;
        border-radius: 6px;
        line-height: 1;
        opacity: 0;
        transform: scale(0);
        filter: blur(4px);
        animation: pillIn 0.4s cubic-bezier(0.23, 1, 0.32, 1)
            var(--pill-delay, 0.6s) forwards;
    }

    @media (max-width: 1199px) {
        .pill {
            font-size: 9px;
            padding: 2px 8px;
        }
    }

    @media (max-width: 809px) {
        .pill {
            font-size: 8px;
            padding: 2px 6px;
            border-radius: 4px;
        }
    }

    .pill-red {
        --pill-delay: 0.6s;
        background: linear-gradient(135deg, #f87171 0%, #ef4444 100%);
        color: white;
        box-shadow: 0 2px 8px rgba(248, 113, 113, 0.2);
        animation:
            pillIn 0.4s cubic-bezier(0.23, 1, 0.32, 1) var(--pill-delay)
                forwards,
            pillGlow 0.6s ease-in-out 1.1s 1;
    }

    .pill-blue {
        --pill-delay: 0.75s;
        background: rgba(59, 130, 246, 0.12);
        color: #60a5fa;
        border: 1px solid rgba(59, 130, 246, 0.2);
        font-weight: 500;
        letter-spacing: 0.04em;
    }

    .pill-amber {
        --pill-delay: 0.9s;
        background: rgba(250, 204, 21, 0.1);
        color: #fbbf24;
        border: 1px solid rgba(250, 204, 21, 0.15);
        font-weight: 500;
        letter-spacing: 0.04em;
    }

    .pill-lavender {
        --pill-delay: 1.05s;
        background: rgba(185, 154, 255, 0.1);
        color: #b99aff;
        border: 1px solid rgba(185, 154, 255, 0.15);
        font-weight: 500;
        letter-spacing: 0.04em;
        font-size: 8px;
        padding: 2px 6px;
        border-radius: 3px;
    }

    @media (max-width: 1199px) {
        .pill-lavender {
            font-size: 7px;
            padding: 2px 5px;
        }
    }

    @media (max-width: 809px) {
        .pill-lavender {
            font-size: 6px;
            padding: 1px 4px;
        }
    }

    .email-subject {
        font-size: 12px;
        font-weight: 500;
        color: var(--duet-muted);
        letter-spacing: -0.01em;
        margin-bottom: 4px;
    }

    .email-subject-strong {
        font-size: 13px;
        color: var(--duet-fg);
        margin-bottom: 5px;
    }

    .email-subject-dim {
        font-size: 11px;
        color: #52525b;
        margin-bottom: 0;
    }

    .email-subject-faint {
        font-size: 10px;
        color: #3f3f46;
        margin-bottom: 0;
    }

    @media (max-width: 809px) {
        .email-subject {
            font-size: 10px;
        }

        .email-subject-strong {
            font-size: 11px;
            margin-bottom: 3px;
        }

        .email-subject-dim {
            font-size: 9px;
        }

        .email-subject-faint {
            font-size: 8px;
        }
    }

    .email-preview {
        font-size: 12px;
        color: var(--duet-muted);
        line-height: 1.5;
        margin-bottom: 14px;
    }

    .email-preview-dim {
        color: #52525b;
        margin-bottom: 0;
    }

    @media (max-width: 809px) {
        .email-preview {
            font-size: 10px;
            margin-bottom: 10px;
        }

        .email-preview-dim {
            font-size: 9px;
        }
    }

    .autodraft {
        border-radius: 12px;
        padding: 12px 14px;
        background: linear-gradient(
            135deg,
            rgba(251, 146, 60, 0.1) 0%,
            rgba(245, 158, 11, 0.08) 100%
        );
        border: 1px solid rgba(251, 146, 60, 0.2);
        max-height: 0;
        opacity: 0;
        overflow: hidden;
        transform: translateY(8px) scale(0.96);
        animation: autodraftIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) 1.8s
            forwards;
    }

    .autodraft-label {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 6px;
        font-size: 10px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: #fb923c;
    }

    .autodraft-dot {
        width: 6px;
        height: 6px;
        border-radius: 9999px;
        background: rgba(251, 146, 60, 0.7);
        box-shadow: 0 0 10px rgba(251, 146, 60, 0.5);
    }

    .autodraft-text {
        font-size: 12px;
        color: var(--duet-fg);
        line-height: 1.5;
        overflow: hidden;
        position: relative;
    }

    .typing-line {
        background: linear-gradient(90deg, var(--duet-fg) 50%, transparent 50%);
        background-size: 200% 100%;
        background-position: 100% 0;
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        animation: typingReveal 2.4s steps(80, end) 2.2s forwards;
    }

    @keyframes typingReveal {
        to {
            background-position: 0% 0;
        }
    }


    @media (max-width: 1199px) {
        .autodraft-text {
            font-size: 11px;
        }
    }

    @media (max-width: 809px) {
        .autodraft {
            padding: 10px 12px;
            border-radius: 10px;
        }

        .autodraft-text {
            font-size: 10px;
        }
    }

    @keyframes frameBorderPulse {
        0%,
        100% {
            opacity: 0.4;
        }
        50% {
            opacity: 0.6;
        }
    }

    @keyframes frameGlowPulse {
        0%,
        100% {
            opacity: 0.3;
        }
        50% {
            opacity: 0.5;
        }
    }

    @keyframes cornerPulse {
        0%,
        100% {
            opacity: 0.4;
        }
        50% {
            opacity: 0.7;
        }
    }

    @keyframes frameFadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes emailIn {
        from {
            opacity: 0;
            transform: translateY(16px);
        }
        to {
            opacity: var(--final-opacity);
            transform: translateY(0);
        }
    }

    @keyframes pillIn {
        from {
            opacity: 0;
            transform: scale(0);
            filter: blur(4px);
        }
        to {
            opacity: 1;
            transform: scale(1);
            filter: blur(0px);
        }
    }

    @keyframes pillGlow {
        0%,
        100% {
            box-shadow: 0 2px 8px rgba(248, 113, 113, 0.2);
        }
        50% {
            box-shadow: 0 2px 16px rgba(248, 113, 113, 0.45);
        }
    }

    @keyframes autodraftIn {
        from {
            max-height: 0;
            opacity: 0;
            transform: translateY(8px) scale(0.96);
        }
        to {
            max-height: 220px;
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    @keyframes windowIn {
        from {
            opacity: 0;
            transform: translateY(24px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @media (max-width: 809px) {
        @keyframes windowIn {
            from {
                opacity: 0;
                transform: translateY(16px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    }

    @media (prefers-reduced-motion: reduce) {
        [data-animate],
        .callout {
            animation: none !important;
        }

        .callout {
            opacity: 1 !important;
            transform: none !important;
        }

        .email {
            opacity: var(--final-opacity) !important;
            transform: none !important;
        }

        .pill {
            opacity: 1 !important;
            transform: none !important;
            filter: none !important;
        }

        .autodraft {
            max-height: 220px !important;
            opacity: 1 !important;
            transform: none !important;
        }

        .typing-line {
            animation: none !important;
            background: none !important;
            color: var(--duet-fg) !important;
        }

        .window {
            opacity: 1 !important;
            transform: none !important;
        }

        .luminous-frame {
            opacity: 1 !important;
        }
    }
</style>
